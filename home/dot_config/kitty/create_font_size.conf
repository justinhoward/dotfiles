# Per-machine kitty font size (kitty.conf does `include font_size.conf`).
# chezmoi seeds this once and never overwrites it, so just edit the value here
# on each machine — `chezmoi apply` leaves it alone. Default: 13.0.
font_size 13.0
