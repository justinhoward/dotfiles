# Applied automatically when the OS reports light mode.
# Light = Everforest, matching nvim's light colorscheme (see nvim colorscheme.lua)
# so the terminal, editor, bat, and delta all switch together.
include themes/everforest-light.conf
