" Persistent undo. Swap, undo, and backup directories default to nvim's XDG
" state location (~/.local/state/nvim/{swap,undo,backup}//) and are created
" automatically — no ~/.vim needed.
set undofile
