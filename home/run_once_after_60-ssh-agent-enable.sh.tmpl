{{- if eq .chezmoi.os "linux" -}}
#!/bin/sh
# Enable + start the ssh-agent user service whose unit chezmoi just installed.
command -v systemctl >/dev/null 2>&1 || exit 0
systemctl --user enable --now ssh-agent.service 2>/dev/null || true
{{- end -}}
